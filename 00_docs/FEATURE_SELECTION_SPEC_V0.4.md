# Feature Selection Spec V0.4 — Work9

Reuse the V0.4 selection algorithm on freshly rebuilt V012/V013 features. PRIMARY validation is current-active Base SKU × current-active Branch × known Pair × target available. SECONDARY historical validation is diagnostic only. No Apr-2026+ target is used for development selection. No feature is inherited merely because it was selected in Work8; the selected list is regenerated.
